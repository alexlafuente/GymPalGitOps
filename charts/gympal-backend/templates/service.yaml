apiVersion: v1
kind: Service
metadata:
  name: {{ include "gympal-backend.fullname" . }}
  labels:
    {{- include "gympal-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gympal-backend.selectorLabels" . | nindent 4 }}
